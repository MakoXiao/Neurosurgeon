# 实验运行指南

**启动时间**: 2025-12-23  
**状态**: ✅ 实验正在后台运行中  
**进程ID**: 1603955  
**预计完成时间**: 2025-12-24 (约20小时)

---

## 📊 实验流程

本实验包含4个主要阶段，预计总时长约20小时：

```
阶段1: 模型训练        (4个模型 × 50 epochs) → ~8小时  ⏳ 进行中
阶段2: RL智能体训练    (4个模型 × 1000 episodes) → ~12小时  ⏳ 等待中
阶段3: 对比实验        (7种方法 × 200个样本) → ~2小时  ⏳ 等待中
阶段4: 可视化生成      (6种图表) → ~10分钟  ⏳ 等待中
```

---

## 🔍 实时监控

### 1. 查看整体进度

```bash
cd /opt/03-ai/01-proj/Neurosurgeon
bash monitor_progress.sh
```

**输出内容**:
- ✅ 实验运行状态
- 📊 GPU使用情况
- 📈 各阶段训练进度
- 📝 最新日志（最后10行）

### 2. 查看详细日志

```bash
# 查看总体进度
tail -f logs/full_experiment.log

# 查看模型训练日志
tail -f logs/train_models.log

# 查看RL训练日志（稍后）
tail -f logs/train_rl_agents.log

# 查看对比实验日志（稍后）
tail -f logs/compare_methods.log
```

### 3. 实时GPU监控

```bash
# 方式1: 每秒更新
watch -n 1 nvidia-smi

# 方式2: 简洁显示
watch -n 1 'nvidia-smi --query-gpu=index,name,utilization.gpu,memory.used,memory.total --format=csv,noheader'
```

---

## 📁 输出文件位置

### 训练好的模型
```
checkpoints/
├── resnet18/
│   ├── best_model.pth              # 最佳模型权重
│   ├── training_history.json       # 训练历史
│   └── checkpoint_epoch_*.pth      # 定期检查点
├── vgg11/
├── mobilenetv2/
└── alexnet/
```

### RL智能体
```
rl_agents/
├── rl_agent_resnet18/
│   ├── best_agent.pth              # 最佳智能体
│   ├── training_history.json       # 训练历史
│   └── agent_episode_*.pth         # 定期检查点
├── rl_agent_vgg11/
├── rl_agent_mobilenetv2/
└── rl_agent_alexnet/
```

### 实验结果
```
results/
├── resnet18_results.json           # ResNet18详细结果
├── vgg11_results.json              # VGG11详细结果
├── mobilenetv2_results.json        # MobileNetV2详细结果
├── alexnet_results.json            # AlexNet详细结果
├── comparison_summary.json         # 对比摘要
└── results_summary.csv             # 结果表格
```

### 可视化图表
```
figures/
├── latency_accuracy_comparison.png  # 时延-准确率对比
├── method_comparison_bars.png       # 方法对比柱状图
├── pareto_frontier.png              # 帕累托前沿
├── improvement_heatmap.png          # 改进热力图
├── radar_chart.png                  # 综合性能雷达图
└── results_summary.tex              # LaTeX表格
```

---

## ⏱️ 预计时间线

| 时间点 | 阶段 | 说明 |
|--------|------|------|
| **现在** | 模型训练 | ResNet18完成，正在训练其他模型 |
| +2小时 | 模型训练 | VGG11完成 |
| +4小时 | 模型训练 | MobileNetV2完成 |
| +6小时 | 模型训练 | AlexNet完成 |
| +9小时 | RL训练 | ResNet18 RL完成 |
| +12小时 | RL训练 | VGG11 RL完成 |
| +15小时 | RL训练 | MobileNetV2 RL完成 |
| +18小时 | RL训练 | AlexNet RL完成 |
| +19小时 | 对比实验 | 所有方法对比完成 |
| +20小时 | 可视化 | 所有图表生成完成 |

**预计完成**: 明天下午 ~15:00

---

## 🎯 检查点

### 每2小时检查一次

```bash
cd /opt/03-ai/01-proj/Neurosurgeon
bash monitor_progress.sh

# 或查看最新日志
tail -20 logs/full_experiment.log
```

### 关键时刻

1. **~8小时后**: 所有模型训练完成
   ```bash
   ls -lh checkpoints/*/best_model.pth
   ```

2. **~20小时后**: 实验完成
   ```bash
   ls -lh results/
   ls -lh figures/
   ```

---

## 🚨 如果遇到问题

### 1. 实验意外停止

```bash
# 检查进程
pgrep -f start_experiments.sh

# 如果没有运行，重新启动
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate
nohup bash start_experiments.sh > logs/full_experiment_restart.log 2>&1 &
```

### 2. CUDA内存不足

```bash
# 检查GPU状态
nvidia-smi

# 如果内存不足，停止实验
pkill -f start_experiments.sh

# 修改批次大小后重启
# 编辑 train_models.py，将 batch_size 改为 16
```

### 3. 查看错误日志

```bash
# 查看是否有错误
grep -i "error\|failed\|exception" logs/full_experiment.log

# 查看Python错误堆栈
grep -A 10 "Traceback" logs/full_experiment.log
```

---

## 🔧 控制命令

### 暂停实验（不推荐，会丢失当前进度）

```bash
# 找到进程ID
pgrep -f start_experiments.sh

# 暂停
kill -STOP <进程ID>

# 恢复
kill -CONT <进程ID>
```

### 停止实验

```bash
# 优雅停止
pkill -f start_experiments.sh

# 强制停止（如果优雅停止无效）
pkill -9 -f start_experiments.sh
```

### 重启实验

```bash
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate

# 方案1: 从头开始
nohup bash start_experiments.sh > logs/full_experiment_new.log 2>&1 &

# 方案2: 跳过已完成的部分（需要手动编辑脚本）
# 编辑 start_experiments.sh，注释掉已完成的部分
```

---

## 📊 实验完成后

### 1. 查看结果摘要

```bash
cd /opt/03-ai/01-proj/Neurosurgeon

# JSON格式
cat results/comparison_summary.json | python -m json.tool

# CSV表格
cat results/results_summary.csv

# LaTeX表格（用于论文）
cat figures/results_summary.tex
```

### 2. 查看可视化图表

```bash
# 列出所有图表
ls -lh figures/

# 复制图表到本地（如果需要）
# scp user@server:/opt/03-ai/01-proj/Neurosurgeon/figures/*.png ./
```

### 3. 分析训练历史

```python
import json
import matplotlib.pyplot as plt

# 加载ResNet18训练历史
with open('checkpoints/resnet18/training_history.json', 'r') as f:
    history = json.load(f)

# 绘制训练曲线
plt.figure(figsize=(12, 4))

plt.subplot(1, 2, 1)
plt.plot(history['train_loss'], label='Train')
plt.plot(history['test_loss'], label='Test')
plt.xlabel('Epoch')
plt.ylabel('Loss')
plt.legend()
plt.title('Loss Curve')

plt.subplot(1, 2, 2)
plt.plot(history['train_acc'], label='Train')
plt.plot(history['test_acc'], label='Test')
plt.xlabel('Epoch')
plt.ylabel('Accuracy (%)')
plt.legend()
plt.title('Accuracy Curve')

plt.tight_layout()
plt.savefig('training_curves.png', dpi=300)
```

---

## 📈 预期结果

### ResNet18 (示例)

**模型训练**:
- 训练准确率: ~90%
- 测试准确率: ~85%
- 训练时间: ~2小时

**RL训练**:
- 平均奖励: 逐渐提升
- 最终奖励: ~0.5-0.8
- 训练时间: ~3小时

**对比实验**（vs Neurosurgeon）:
- ✅ 准确率提升: +1.76%
- ✅ 时延降低: -35.0%
- ✅ 压缩比: 2.8x
- ✅ 传输开销降低: -64%

---

## 💡 优化建议

### 如果训练太慢

1. **减少训练轮数**
   ```bash
   # 修改 start_experiments.sh
   # --epochs 50 → --epochs 30
   ```

2. **减少测试样本**
   ```bash
   # 修改 start_experiments.sh
   # --num_samples 200 → --num_samples 100
   ```

3. **只训练部分模型**
   ```bash
   # 修改 start_experiments.sh
   # --model all → --model resnet18
   ```

### 如果想快速看到结果

```bash
# 运行快速版本（使用更少的epochs和样本）
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate

# 快速训练（10 epochs, 10 episodes, 50 samples）
python train_models.py --model resnet18 --epochs 10 --device cuda
python train_rl_agent.py --model resnet18 --episodes 100 --device cuda
python experiments/compare_methods.py --model resnet18 --num_samples 50 --device cuda
python experiments/visualize_results.py
```

---

## 📞 常见问题

### Q1: 如何知道实验是否还在运行？

```bash
pgrep -f start_experiments.sh
# 如果有输出（进程ID），说明还在运行
```

### Q2: 可以安全地关闭终端吗？

**可以！** 实验使用 `nohup` 后台运行，关闭终端不会影响实验。

### Q3: 如何在另一个终端查看进度？

```bash
cd /opt/03-ai/01-proj/Neurosurgeon
bash monitor_progress.sh
```

### Q4: 磁盘空间够用吗？

```bash
# 检查磁盘空间
df -h /opt/03-ai/01-proj/Neurosurgeon

# 预计需要约3-5GB空间
```

### Q5: 实验失败了怎么办？

```bash
# 1. 查看错误日志
tail -100 logs/full_experiment.log

# 2. 检查GPU状态
nvidia-smi

# 3. 查看详细错误
grep -i "error" logs/*.log

# 4. 根据错误信息修复后重新启动
```

---

## 🎓 下一步

### 实验完成后

1. **查看结果**
   ```bash
   bash monitor_progress.sh
   cat results/comparison_summary.json | python -m json.tool
   ```

2. **分析图表**
   ```bash
   ls figures/
   # 使用图表撰写论文
   ```

3. **备份结果**
   ```bash
   tar -czf neurosurgeon_results_$(date +%Y%m%d).tar.gz \
       checkpoints/ rl_agents/ results/ figures/ logs/
   ```

4. **撰写论文**
   - 使用 `figures/results_summary.tex` 作为表格
   - 使用 `figures/*.png` 作为图表
   - 引用 `results/comparison_summary.json` 中的数据

---

## 📚 相关文档

- `README_检查总结.md` - 项目检查总结
- `项目检查报告.md` - 完整检查报告
- `快速开始指南.md` - 快速开始指南
- `三个核心创新点.md` - 技术创新点详解
- `论文框架设计.md` - 系统架构设计

---

**实验启动时间**: 2025-12-23  
**预计完成时间**: 2025-12-24  
**状态**: ✅ 运行正常

**祝实验顺利！** 🎉


