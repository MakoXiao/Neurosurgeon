# 命令速查表

**实验进程ID**: 1603955  
**启动时间**: 2025-12-23  
**预计完成**: 2025-12-24 下午

---

## 🔍 监控命令

### 查看整体进度（推荐）
```bash
cd /opt/03-ai/01-proj/Neurosurgeon
bash monitor_progress.sh
```

### 查看实时日志
```bash
# 总体日志
tail -f logs/full_experiment.log

# 模型训练日志
tail -f logs/train_models.log

# RL训练日志
tail -f logs/train_rl_agents.log

# 对比实验日志
tail -f logs/compare_methods.log
```

### GPU监控
```bash
# 实时更新
watch -n 1 nvidia-smi

# 简洁显示
watch -n 1 'nvidia-smi --query-gpu=index,name,utilization.gpu,memory.used,memory.total --format=csv,noheader'
```

### 检查进程
```bash
# 查看是否运行
pgrep -f start_experiments.sh

# 查看详细信息
ps aux | grep start_experiments.sh
```

---

## 📊 查看结果

### 检查已完成的模型
```bash
# 列出模型权重
ls -lh checkpoints/*/best_model.pth

# 查看训练历史
cat checkpoints/resnet18/training_history.json | python -m json.tool
```

### 检查RL智能体
```bash
# 列出智能体
ls -lh rl_agents/*/best_agent.pth

# 查看训练历史
cat rl_agents/rl_agent_resnet18/training_history.json | python -m json.tool
```

### 查看实验结果
```bash
# 详细结果
cat results/resnet18_results.json | python -m json.tool

# 对比摘要
cat results/comparison_summary.json | python -m json.tool

# CSV表格
cat results/results_summary.csv
```

### 查看图表
```bash
# 列出图表
ls -lh figures/

# 使用图片查看器
# display figures/latency_accuracy_comparison.png  # Linux
# open figures/latency_accuracy_comparison.png     # Mac
```

---

## 🎮 控制命令

### 停止实验
```bash
# 优雅停止
pkill -f start_experiments.sh

# 强制停止（如果需要）
pkill -9 -f start_experiments.sh
```

### 重新启动
```bash
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate
nohup bash start_experiments.sh > logs/full_experiment_restart.log 2>&1 &
```

---

## 📁 目录结构

```bash
# 查看所有输出
tree -L 2 checkpoints/
tree -L 2 rl_agents/
ls -lh results/
ls -lh figures/

# 查看磁盘使用
du -sh checkpoints/ rl_agents/ results/ figures/ logs/
```

---

## 🔧 故障排除

### 查看错误日志
```bash
# 搜索错误
grep -i "error" logs/full_experiment.log

# 搜索异常
grep -i "exception" logs/full_experiment.log

# 查看Python堆栈
grep -A 10 "Traceback" logs/full_experiment.log
```

### 检查资源使用
```bash
# CPU使用
top -p $(pgrep -f python)

# GPU使用
nvidia-smi

# 内存使用
free -h

# 磁盘空间
df -h /opt/03-ai/01-proj/Neurosurgeon
```

---

## 📊 快速统计

### 训练进度统计
```bash
# 统计已完成的模型
ls checkpoints/*/best_model.pth 2>/dev/null | wc -l

# 统计已完成的RL智能体
ls rl_agents/*/best_agent.pth 2>/dev/null | wc -l

# 统计已生成的结果
ls results/*.json 2>/dev/null | wc -l

# 统计已生成的图表
ls figures/*.png 2>/dev/null | wc -l
```

### 日志统计
```bash
# 日志文件大小
ls -lh logs/

# 日志行数
wc -l logs/*.log

# 最近更新的日志
ls -lt logs/
```

---

## 🎯 关键时刻

### 实验开始后 (~2小时)
```bash
bash monitor_progress.sh
# 应该看到: ✓ resnet18 - 已完成, ⏳ vgg11 - 训练中...
```

### 模型训练完成后 (~8小时)
```bash
ls checkpoints/*/best_model.pth
# 应该看到4个模型都已完成
```

### RL训练完成后 (~20小时)
```bash
ls rl_agents/*/best_agent.pth
# 应该看到4个智能体都已完成
```

### 全部完成后 (~20小时)
```bash
ls figures/
# 应该看到6个PNG图表和1个TEX文件
```

---

## 📈 Python快速查看

### 查看训练曲线
```python
import json
import matplotlib.pyplot as plt

# 加载历史
with open('checkpoints/resnet18/training_history.json', 'r') as f:
    hist = json.load(f)

# 绘制
plt.figure(figsize=(10, 4))
plt.subplot(1, 2, 1)
plt.plot(hist['train_loss'], label='Train')
plt.plot(hist['test_loss'], label='Test')
plt.legend(); plt.title('Loss')

plt.subplot(1, 2, 2)
plt.plot(hist['train_acc'], label='Train')
plt.plot(hist['test_acc'], label='Test')
plt.legend(); plt.title('Accuracy')
plt.show()
```

### 查看实验结果
```python
import json
import pandas as pd

# 加载结果
with open('results/comparison_summary.json', 'r') as f:
    results = json.load(f)

# 转换为DataFrame
data = []
for model, methods in results.items():
    for method, metrics in methods.items():
        data.append({
            'Model': model,
            'Method': method,
            'Latency': metrics['avg_latency'],
            'Accuracy': metrics['avg_accuracy']
        })

df = pd.DataFrame(data)
print(df)

# 保存为Excel
df.to_excel('results_summary.xlsx', index=False)
```

---

## 🚀 一键命令

### 完整监控仪表板（推荐）
```bash
watch -n 5 'cd /opt/03-ai/01-proj/Neurosurgeon && bash monitor_progress.sh'
```

### 分屏监控（如果使用tmux）
```bash
# 启动tmux
tmux

# 创建分屏
# Ctrl+B, %  (左右分屏)
# Ctrl+B, "  (上下分屏)

# 左侧: 查看日志
tail -f logs/full_experiment.log

# 右侧: 监控GPU
watch -n 1 nvidia-smi

# 切换窗口: Ctrl+B, 方向键
```

---

## 📧 实验完成通知（可选）

### 设置邮件通知
```bash
# 在 start_experiments.sh 末尾添加
echo "实验完成时间: $(date)" | mail -s "Neurosurgeon实验完成" your@email.com
```

### Slack/钉钉通知
```bash
# 在 start_experiments.sh 末尾添加
curl -X POST -H 'Content-type: application/json' \
  --data '{"text":"Neurosurgeon实验已完成！"}' \
  YOUR_WEBHOOK_URL
```

---

## 💾 备份命令

### 备份所有结果
```bash
cd /opt/03-ai/01-proj/Neurosurgeon
tar -czf neurosurgeon_results_$(date +%Y%m%d_%H%M%S).tar.gz \
    checkpoints/ rl_agents/ results/ figures/ logs/

# 查看备份大小
ls -lh neurosurgeon_results_*.tar.gz
```

### 只备份关键文件
```bash
tar -czf neurosurgeon_key_results.tar.gz \
    checkpoints/*/best_model.pth \
    rl_agents/*/best_agent.pth \
    results/*.json \
    figures/*.png \
    figures/*.tex
```

---

## 🎓 最常用命令（推荐收藏）

```bash
# 1. 查看进度
bash monitor_progress.sh

# 2. 查看日志
tail -f logs/full_experiment.log

# 3. 查看GPU
watch -n 1 nvidia-smi

# 4. 检查完成情况
ls checkpoints/*/best_model.pth
ls rl_agents/*/best_agent.pth
ls results/*.json
ls figures/*.png

# 5. 查看结果
cat results/comparison_summary.json | python -m json.tool
```

---

**快速访问**:
```bash
cd /opt/03-ai/01-proj/Neurosurgeon  # 进入项目目录
bash monitor_progress.sh             # 查看进度
```

**实验状态**: ✅ 运行中  
**进程ID**: 1603955  
**预计完成**: 2025-12-24


