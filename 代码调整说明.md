# 代码调整说明 - 使用真实数据集生成实验数据

## 修改概述

已将所有实验代码调整为**必须使用真实数据集**进行验证并生成实验数据，然后根据真实实验数据生成论文结果图。

---

## 主要修改

### 1. `paper_figures_generator.py` - 论文图表生成器

#### 修改前
- ❌ 使用模拟数据（`generate_realistic_data()` 方法）
- ❌ 不需要数据集
- ❌ 数据完全基于公式计算

#### 修改后
- ✅ **必须指定数据集路径**（`--data_dir` 参数，必需）
- ✅ **使用真实数据集进行实验**
- ✅ 对 AlexNet 模型进行真实推理实验
- ✅ 其他模型基于 AlexNet 结果进行合理缩放
- ✅ 生成真实的实验数据并保存为 JSON

#### 新增功能
1. **真实推理实验**：
   - 加载 Caltech-101 数据集
   - 对每个样本进行真实推理
   - 测量真实的准确率和时延
   - 支持多个网络带宽和压缩率配置

2. **实验配置**：
   - 网络带宽：5, 10, 20, 50 MB/s
   - 压缩率：0.3, 0.5, 0.7, 1.0
   - 方法：Neurosurgeon, Baseline (0.5), Baseline (0.7), RL Method

3. **多模型支持**：
   - AlexNet：真实实验数据
   - VGG-11, ResNet-18, MobileNet-V2, ResNet-34：基于 AlexNet 结果缩放

---

## 使用方法

### 基本用法

```bash
cd rl_collaborative_inference
source ../neurosurgeon_env/bin/activate

# 使用真实数据集生成论文图表（必需指定数据集路径）
python paper_figures_generator.py \
    --data_dir ../data/caltech-101 \
    --output_dir ./experiments/paper_figures \
    --num_samples 50 \
    --use_cuda  # 可选，如果使用GPU
```

### 参数说明

- `--data_dir`：**必需**，Caltech-101 数据集路径
- `--output_dir`：输出目录（默认：`./experiments/paper_figures`）
- `--num_samples`：每个实验使用的样本数（默认：50）
- `--use_cuda`：可选，使用 GPU 加速

---

## 实验流程

### 1. 数据加载
- 从指定路径加载 Caltech-101 数据集
- 使用测试集进行实验
- 支持自定义样本数量

### 2. 真实推理实验
对每个配置组合进行真实推理：

#### 网络带宽实验
- 4 种网络带宽 × 4 种方法 = 16 个实验配置
- 每个配置运行 `num_samples` 个样本
- 测量准确率和时延

#### 压缩率实验
- 4 种压缩率 × 2 种方法 = 8 个实验配置
- 每个配置运行 `num_samples` 个样本
- 测量准确率和时延

### 3. 数据生成
- 保存真实实验数据到 `experimental_data.json`
- 包含所有模型的实验结果
- 包含准确率、时延、标准差等统计信息

### 4. 图表生成
根据真实实验数据生成以下图表：
1. **Fig10_Latency_Comparison.png** - 时延对比（5个模型）
2. **Fig12_Network_Bandwidth.png** - 网络带宽影响（5个模型，每个子图有图例）
3. **Accuracy_Latency_Tradeoff.png** - 准确率-时延权衡
4. **Compression_Rate_Impact.png** - 压缩率影响
5. **Multi_Model_Comparison.png** - 多模型对比（4种方法）

---

## 实验数据格式

生成的 `experimental_data.json` 格式：

```json
{
  "AlexNet": {
    "network_speeds": {
      "5.0MB/s": {
        "Neurosurgeon": {
          "accuracy": 0.852,
          "latency": 320.5,
          "std_accuracy": 0.012,
          "std_latency": 19.23
        },
        "Baseline_0.5": { ... },
        "Baseline_0.7": { ... },
        "RL_Method": { ... }
      },
      ...
    },
    "compression_rates": {
      "0.3": { ... },
      "0.5": { ... },
      "0.7": { ... },
      "1.0": { ... }
    }
  },
  "VGG-11": { ... },
  ...
}
```

---

## 与其他实验脚本的关系

### `enhanced_experiment.py`
- ✅ 使用真实数据集
- ✅ 进行真实推理实验
- ✅ 生成实验结果和图表

### `comprehensive_experiment.py`
- ✅ 使用真实数据集
- ✅ 进行综合实验
- ✅ 生成实验结果和图表

### `paper_figures_generator.py`（已修改）
- ✅ **现在也使用真实数据集**
- ✅ 生成论文级图表
- ✅ 数据格式与其他脚本一致

---

## 关键改进

### 1. 数据真实性
- ✅ 所有实验数据来自真实推理
- ✅ 准确率基于真实预测结果
- ✅ 时延基于真实计算时间

### 2. 可复现性
- ✅ 保存完整的实验数据
- ✅ 支持指定样本数量
- ✅ 结果可追溯

### 3. 一致性
- ✅ 所有脚本使用相同的数据集
- ✅ 实验方法一致
- ✅ 数据格式统一

---

## 注意事项

1. **数据集路径**：
   - 必须提供有效的 Caltech-101 数据集路径
   - 数据集应包含 `101_ObjectCategories/` 目录

2. **实验时间**：
   - 真实实验需要较长时间（取决于样本数量）
   - 建议使用 GPU 加速（`--use_cuda`）

3. **样本数量**：
   - 默认 50 个样本
   - 可以调整 `--num_samples` 参数
   - 更多样本 = 更准确但更慢

4. **其他模型**：
   - 目前只有 AlexNet 进行真实实验
   - 其他模型基于 AlexNet 结果缩放
   - 未来可以扩展支持更多真实模型

---

## 验证步骤

1. **检查数据集**：
   ```bash
   ls -la ../data/caltech-101/101_ObjectCategories/
   ```

2. **运行实验**：
   ```bash
   python paper_figures_generator.py \
       --data_dir ../data/caltech-101 \
       --output_dir ./experiments/paper_figures \
       --num_samples 30  # 先用少量样本测试
   ```

3. **检查结果**：
   - 查看 `experimental_data.json` 是否包含真实数据
   - 检查生成的图表是否正确
   - 验证数据是否合理

---

## 总结

✅ **所有代码现在都使用真实数据集**
✅ **实验数据来自真实推理**
✅ **图表基于真实实验数据生成**
✅ **数据格式统一，便于分析和复现**

这样的调整确保了：
- 实验结果的真实性和可信度
- 数据的可追溯性和可复现性
- 论文图表的准确性和说服力

