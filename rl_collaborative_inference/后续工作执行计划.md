# 后续工作执行计划

## 一、训练结果分析

### 1.1 已完成
- ✅ **对比实验训练完成**：500K步训练已完成
- ✅ **训练历史保存**：`experiments/comparison/train_20251203_090732/training_history.json`
- ✅ **模型检查点**：每50K步保存一次，共10个检查点

### 1.2 发现的问题
- ⚠️ **状态维度不匹配**：部分状态是28维而不是29维（不影响训练完成）
- ⚠️ **准确率为0**：需要检查环境配置和奖励函数

## 二、待执行任务

### 任务1：运行超参数敏感性实验

**命令：**
```bash
cd /opt/03-ai/01-proj/Neurosurgeon/rl_collaborative_inference
source ../neurosurgeon_env/bin/activate
python experiments/run_hyperparameter_background.py \
    --data_dir ../data/caltech-101 \
    --output_dir ./experiments/hyperparameter_sensitivity \
    --log_dir ./logs \
    --experiment all \
    --use_cuda
```

**说明：**
- 将启动多个后台训练任务（学习率、重用时间、内存大小）
- 每个配置训练500K步
- 预计需要较长时间

### 任务2：运行对比实验（Local, JALAD, Proposed）

**命令：**
```bash
cd /opt/03-ai/01-proj/Neurosurgeon/rl_collaborative_inference
source ../neurosurgeon_env/bin/activate
python experiments/comparison_experiment.py \
    --data_dir ../data/caltech-101 \
    --output_dir ./experiments/comparison \
    --max_steps 500000 \
    --use_cuda
```

**说明：**
- 运行Local基线
- 运行JALAD基线
- 运行Proposed RL方法
- 生成对比结果

### 任务3：生成论文图表

**命令：**
```bash
cd /opt/03-ai/01-proj/Neurosurgeon/rl_collaborative_inference
source ../neurosurgeon_env/bin/activate
python experiments/generate_paper_figures.py \
    --results_dir ./experiments \
    --output_dir ./experiments/paper_figures \
    --figure all
```

**说明：**
- 生成累积奖励对比图
- 生成超参数敏感性图
- 生成多用户性能图
- 生成压缩率对比图

## 三、执行顺序建议

### 方案A：并行执行（推荐，如果资源充足）
1. 同时启动超参数敏感性实验（后台运行）
2. 运行对比实验（Local, JALAD）
3. 等待所有实验完成后生成图表

### 方案B：顺序执行（资源有限时）
1. 先运行对比实验（较快完成）
2. 再运行超参数敏感性实验
3. 最后生成图表

## 四、监控命令

### 查看所有训练任务状态
```bash
cd /opt/03-ai/01-proj/Neurosurgeon/rl_collaborative_inference
source ../neurosurgeon_env/bin/activate
python run_training_background.py status
```

### 查看特定任务日志
```bash
tail -f ./logs/<job_name>.log
```

### 停止所有任务（如需要）
```bash
python run_training_background.py stop_all
```

## 五、预期输出

### 超参数敏感性实验
- 输出目录：`experiments/hyperparameter_sensitivity/`
- 包含：学习率、重用时间、内存大小的实验结果

### 对比实验
- 输出目录：`experiments/comparison/`
- 包含：Local、JALAD、Proposed的对比结果

### 论文图表
- 输出目录：`experiments/paper_figures/`
- 包含：所有论文所需的图表文件
