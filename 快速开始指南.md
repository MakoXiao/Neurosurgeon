# 快速开始指南

**更新时间**: 2025-12-23  
**适用于**: Neurosurgeon项目 - 基于强化学习的协同推理框架

---

## 🎯 项目概述

本项目实现了三个核心创新点：
1. **混合动作空间PPO**: 同时优化分割点和压缩率
2. **可恢复剪枝压缩**: 无需训练的高效压缩方案  
3. **29维状态空间+双目标奖励**: 平衡准确率和时延

目标：在Caltech-101数据集上训练4个模型，对比7种方法的性能

---

## ✅ 环境检查

### 1. 检查环境配置

```bash
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate
python check_environment.py
```

**预期输出**: 所有检查项都显示 ✅

### 2. 确认GPU可用

```bash
nvidia-smi
python -c "import torch; print(f'CUDA可用: {torch.cuda.is_available()}'); print(f'GPU数量: {torch.cuda.device_count()}')"
```

---

## 🚀 三种运行方式

### 方式1: 快速验证 (推荐首次运行) ⏱️ ~30分钟

验证所有模块能正常工作，适合测试和调试。

```bash
# 进入项目目录并激活环境
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate

# 1. 快速训练ResNet18 (2 epochs)
echo "步骤1: 训练ResNet18模型..."
python train_models.py \
    --model resnet18 \
    --epochs 2 \
    --batch_size 32 \
    --device cuda

# 2. 快速训练RL智能体 (10 episodes)
echo "步骤2: 训练RL智能体..."
python train_rl_agent.py \
    --model resnet18 \
    --episodes 10 \
    --max_steps 20 \
    --device cuda

# 3. 快速对比实验 (10个样本)
echo "步骤3: 运行对比实验..."
python experiments/compare_methods.py \
    --model resnet18 \
    --num_samples 10 \
    --device cuda

# 4. 生成可视化
echo "步骤4: 生成可视化图表..."
python experiments/visualize_results.py

echo "✅ 快速验证完成! 检查 figures/ 目录查看生成的图表"
```

**预期结果**:
- `checkpoints/resnet18/best_model.pth` - 训练好的模型
- `rl_agents/rl_agent_resnet18/best_agent.pth` - 训练好的智能体
- `results/resnet18_results.json` - 实验结果
- `figures/*.png` - 可视化图表

---

### 方式2: 单模型完整实验 ⏱️ ~6小时

对单个模型进行完整训练和评估，适合逐步验证。

```bash
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate

# 选择要训练的模型
MODEL_NAME="resnet18"  # 可选: resnet18, vgg11, mobilenetv2, alexnet

# 1. 训练分类模型 (50 epochs, ~2小时)
echo "步骤1/4: 训练${MODEL_NAME}模型..."
python train_models.py \
    --model $MODEL_NAME \
    --epochs 50 \
    --batch_size 32 \
    --lr 0.001 \
    --device cuda 2>&1 | tee logs/train_${MODEL_NAME}.log

# 2. 训练RL智能体 (1000 episodes, ~3小时)
echo "步骤2/4: 训练RL智能体..."
python train_rl_agent.py \
    --model $MODEL_NAME \
    --episodes 1000 \
    --max_steps 100 \
    --update_interval 10 \
    --device cuda 2>&1 | tee logs/train_rl_${MODEL_NAME}.log

# 3. 运行对比实验 (200个样本, ~30分钟)
echo "步骤3/4: 运行对比实验..."
python experiments/compare_methods.py \
    --model $MODEL_NAME \
    --num_samples 200 \
    --device cuda 2>&1 | tee logs/compare_${MODEL_NAME}.log

# 4. 生成可视化
echo "步骤4/4: 生成可视化..."
python experiments/visualize_results.py 2>&1 | tee logs/visualize.log

echo "✅ ${MODEL_NAME}完整实验完成!"
```

**中途可以暂停**:
- 按 `Ctrl+C` 可随时停止
- 已保存的检查点可以继续使用
- 可以跳过已完成的步骤

---

### 方式3: 完整实验 (所有模型) ⏱️ ~20小时

一键运行所有模型的完整实验流程，获取论文级别结果。

```bash
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate

# 创建日志目录
mkdir -p logs

# 运行完整实验脚本
nohup bash run_experiments.sh > logs/full_experiment.log 2>&1 &

# 查看运行进度
tail -f logs/full_experiment.log

# 或者在screen/tmux中运行（推荐）
screen -S neurosurgeon
bash run_experiments.sh
# 按 Ctrl+A+D 退出screen，稍后用 screen -r neurosurgeon 恢复
```

**实验流程**:
1. 训练ResNet18, VGG11, MobileNetV2, AlexNet (4个模型)
2. 为每个模型训练RL智能体
3. 运行所有模型的对比实验
4. 生成所有可视化图表

**预计时间分配**:
- 模型训练: 4 × 2小时 = 8小时
- RL训练: 4 × 3小时 = 12小时  
- 对比实验: 4 × 30分钟 = 2小时
- **总计**: ~22小时

**建议**:
- 在服务器上后台运行
- 使用screen或tmux保持会话
- 定期检查日志和GPU使用情况

---

## 📊 查看结果

### 1. 训练历史

```bash
# 查看模型训练历史
cat checkpoints/resnet18/training_history.json

# 查看RL训练历史  
cat rl_agents/rl_agent_resnet18/training_history.json
```

### 2. 实验结果

```bash
# 查看单个模型的详细结果
cat results/resnet18_results.json | python -m json.tool

# 查看所有模型的对比摘要
cat results/comparison_summary.json | python -m json.tool

# 查看CSV表格
cat results/results_summary.csv
```

### 3. 可视化图表

```bash
# 查看生成的图表
ls -lh figures/

# 图表说明:
# - latency_accuracy_comparison.png  时延-准确率对比
# - method_comparison_bars.png       方法对比柱状图
# - pareto_frontier.png              帕累托前沿
# - improvement_heatmap.png          改进热力图
# - radar_chart.png                  综合性能雷达图
# - results_summary.csv              结果表格
# - results_summary.tex              LaTeX表格
```

### 4. 使用Python查看结果

```python
import json
import pandas as pd

# 加载实验结果
with open('results/comparison_summary.json', 'r') as f:
    results = json.load(f)

# 查看ResNet18的结果
for method, metrics in results['resnet18'].items():
    print(f"{method}:")
    print(f"  时延: {metrics['avg_latency']:.2f} ± {metrics['std_latency']:.2f} ms")
    print(f"  准确率: {metrics['avg_accuracy']:.4f} ± {metrics['std_accuracy']:.4f}")

# 加载表格
df = pd.read_csv('results/results_summary.csv')
print(df)
```

---

## 🔍 监控训练进度

### 1. 实时监控GPU使用

```bash
# 方式1: nvidia-smi实时更新
watch -n 1 nvidia-smi

# 方式2: 使用gpustat (如果已安装)
pip install gpustat
watch -n 1 gpustat -cpu
```

### 2. 查看训练日志

```bash
# 查看最新的训练日志
tail -f logs/train_resnet18.log

# 查看RL训练日志
tail -f logs/train_rl_resnet18.log
```

### 3. 检查训练进度

```bash
# 查看模型检查点
ls -lh checkpoints/resnet18/

# 查看RL检查点
ls -lh rl_agents/rl_agent_resnet18/
```

---

## ⚠️ 常见问题

### 问题1: CUDA内存不足

**错误**: `RuntimeError: CUDA out of memory`

**解决方案**:
```bash
# 减小批次大小
python train_models.py --model resnet18 --batch_size 16 --device cuda

# 或使用CPU (会慢很多)
python train_models.py --model resnet18 --device cpu
```

### 问题2: 数据加载缓慢

**解决方案**:
```bash
# 减少数据加载的worker数量
# 在代码中修改: num_workers=2 或 num_workers=0
```

### 问题3: 训练中断后如何继续

**解决方案**:
```bash
# 模型训练会自动保存检查点
# 重新运行训练命令即可，会从最佳检查点继续

# 如果需要从特定epoch继续，可以修改代码加载检查点
```

### 问题4: 可视化中文显示问题

**错误**: 图表中中文显示为方框

**解决方案**:
```bash
# 安装中文字体
sudo apt-get install fonts-wqy-microhei

# 或在代码中改用英文标签
```

### 问题5: 虚拟环境激活失败

**解决方案**:
```bash
# 检查虚拟环境路径
ls -la /opt/03-ai/01-proj/Neurosurgeon/neurosurgeon_env/

# 重新激活
source /opt/03-ai/01-proj/Neurosurgeon/neurosurgeon_env/bin/activate

# 确认激活成功
which python
# 应显示: /opt/03-ai/01-proj/Neurosurgeon/neurosurgeon_env/bin/python
```

---

## 📝 实验检查清单

### 开始实验前

- [ ] 运行`check_environment.py`确认环境正常
- [ ] 确认GPU可用 (`nvidia-smi`)
- [ ] 确认数据集存在 (`ls data/caltech-101/101_ObjectCategories/`)
- [ ] 创建必要的目录已完成
- [ ] 虚拟环境已激活

### 训练过程中

- [ ] 监控GPU使用率
- [ ] 查看训练日志
- [ ] 检查检查点是否正常保存
- [ ] 验证准确率是否逐渐提升

### 实验完成后

- [ ] 检查所有模型的检查点
- [ ] 检查所有RL智能体
- [ ] 查看实验结果JSON文件
- [ ] 确认生成了所有可视化图表
- [ ] 备份重要结果

---

## 🎯 预期结果

### 训练完成后，应该得到：

**文件结构**:
```
checkpoints/
├── resnet18/
│   ├── best_model.pth
│   ├── training_history.json
│   └── checkpoint_epoch_*.pth
├── vgg11/
├── mobilenetv2/
└── alexnet/

rl_agents/
├── rl_agent_resnet18/
│   ├── best_agent.pth
│   ├── training_history.json
│   └── agent_episode_*.pth
├── rl_agent_vgg11/
├── rl_agent_mobilenetv2/
└── rl_agent_alexnet/

results/
├── resnet18_results.json
├── vgg11_results.json
├── mobilenetv2_results.json
├── alexnet_results.json
├── comparison_summary.json
└── results_summary.csv

figures/
├── latency_accuracy_comparison.png
├── method_comparison_bars.png
├── pareto_frontier.png
├── improvement_heatmap.png
├── radar_chart.png
└── results_summary.tex
```

### 性能指标 (ResNet18示例)

| 方法 | 时延 (ms) | 准确率 | 改进 |
|------|----------|--------|------|
| All Edge | ~400 | 0.850 | Baseline |
| All Cloud | ~300 | 0.850 | Baseline |
| Neurosurgeon | 245 | 0.852 | Baseline |
| Fixed (0.3) | 180 | 0.825 | 快但精度低 |
| Fixed (0.5) | 200 | 0.840 | 平衡 |
| Fixed (0.7) | 220 | 0.845 | 保守 |
| **RL Agent** | **159** | **0.867** | **最优** |

**关键改进** (vs Neurosurgeon):
- ✅ 时延降低: -35.0%
- ✅ 准确率提升: +1.76%
- ✅ 压缩比: 2.8x
- ✅ 传输开销: -64%

---

## 💡 优化建议

### 1. 加速训练

```bash
# 使用更大的批次大小（如果GPU内存足够）
python train_models.py --model resnet18 --batch_size 64 --device cuda

# 使用混合精度训练（需要PyTorch 1.6+）
# 在代码中添加: torch.cuda.amp

# 使用多GPU训练
# 在代码中添加: nn.DataParallel(model)
```

### 2. 提高准确率

```bash
# 增加训练轮数
python train_models.py --model resnet18 --epochs 100

# 使用更小的学习率
python train_models.py --model resnet18 --lr 0.0001

# 增加数据增强
# 在dataset/caltech101_loader.py中添加更多transform
```

### 3. 减少时延

```bash
# 调整环境参数
# 在代码中修改:
# - bandwidth = 200.0  # 增加带宽
# - network_latency = 30.0  # 减少延迟

# 优化压缩率范围
# 在代码中修改:
# - compression_min = 0.2
# - compression_max = 0.8
```

---

## 📚 更多文档

- `项目检查报告.md` - 完整的项目检查报告
- `PROJECT_STATUS.md` - 项目状态和进度
- `三个核心创新点.md` - 技术创新点详解
- `论文框架设计.md` - 系统架构设计
- `check_environment.py` - 环境检查脚本

---

## 🎓 下一步

### 完成快速验证后

1. **分析结果**: 查看生成的图表和结果文件
2. **调整参数**: 根据结果调整训练参数
3. **运行完整实验**: 使用最优参数训练所有模型

### 完成完整实验后

1. **结果分析**: 对比不同模型和方法的性能
2. **论文撰写**: 使用生成的图表和表格
3. **进一步优化**: 根据实验结果改进算法

---

## 🚀 立即开始

**推荐命令** (快速验证):
```bash
cd /opt/03-ai/01-proj/Neurosurgeon
source neurosurgeon_env/bin/activate
python check_environment.py
python train_models.py --model resnet18 --epochs 2 --device cuda
```

**完成后**:
```bash
ls checkpoints/resnet18/
cat checkpoints/resnet18/training_history.json | python -m json.tool
```

**祝实验顺利！** 🎉

---

**文档版本**: 1.0  
**最后更新**: 2025-12-23  
**维护者**: AI Assistant


