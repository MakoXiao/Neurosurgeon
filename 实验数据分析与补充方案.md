# 实验数据分析与补充方案

## 一、当前项目实验数据分析

### 1.1 已有实验内容

根据代码分析，当前项目已实现：

1. **基础训练框架**
   - ✅ PPO算法实现
   - ✅ 单用户协同推理环境
   - ✅ 模型分割和特征压缩
   - ✅ 训练过程记录（episode rewards, accuracies, latencies）

2. **评估实验**
   - ✅ 不同网络带宽下的性能评估（5, 10, 20, 50 MB/s）
   - ✅ 不同压缩率下的性能评估（0.3, 0.5, 0.7, 1.0）
   - ✅ 与Neurosurgeon和固定压缩率基线的对比

3. **生成的图表**
   - ✅ 时延对比图（不同模型、不同网络带宽）
   - ✅ 准确率-时延权衡图
   - ✅ 压缩率影响图

### 1.2 缺失的实验数据

对比论文《Multi-Agent Collaborative Inference via DNN Decoupling: Intermediate Feature Compression and Edge Learning》和附件图片，发现以下缺失：

#### 1.2.1 训练过程曲线缺失

**缺失内容：**
- ❌ **Cumulative Reward vs Time Frame**：训练过程中累积奖励随时间的变化曲线
- ❌ **Value Loss vs Time Frame**：Critic网络的Value Loss变化曲线
- ❌ **训练稳定性分析**：不同方法（Local, JALAD, MAHPPO）的训练曲线对比

**原因分析：**
- 当前`train.py`只记录了episode级别的rewards，没有计算和记录累积奖励
- 没有记录Critic网络的loss值
- 训练步数可能不够（默认10000步），论文中需要500K步的训练

#### 1.2.2 超参数敏感性实验缺失

**缺失内容：**
- ❌ **学习率敏感性**：不同学习率（0.00001, 0.0001, 0.001, 0.01）对性能的影响
- ❌ **重用时间（Reuse Time）敏感性**：不同重用时间（5, 10, 20, 40, 80）的影响
- ❌ **内存大小（Memory Size）敏感性**：不同内存大小（256, 512, 1024, 2048, 4096）的影响
- ❌ **Value Loss vs Memory Size**：不同内存大小对Value Loss的影响

**原因分析：**
- 当前代码没有实现超参数网格搜索或敏感性分析
- PPO算法中的`repeat_time`（重用时间）和buffer大小（内存大小）没有进行系统性的实验

#### 1.2.3 多用户/多智能体实验缺失

**缺失内容：**
- ❌ **不同用户数量下的性能**：3-10个用户的Cumulative Reward对比
- ❌ **多智能体协作机制**：多个边缘设备与云端的协作
- ❌ **多用户场景下的压缩率对比**

**原因分析：**
- 当前环境只支持单用户场景
- 没有实现多智能体PPO（MAHPPO）的完整集成
- 虽然有MAHPPO-main代码，但没有集成到主实验框架中

#### 1.2.4 对比基线缺失

**缺失内容：**
- ❌ **JALAD基线**：论文中提到的JALAD方法没有实现
- ❌ **Local基线**：完全本地推理的基线
- ❌ **MAHPPO完整实现**：虽然有代码，但没有完整的训练和评估流程

**原因分析：**
- JALAD方法需要单独实现（可能是基于自编码器的压缩方法）
- Local基线需要实现完全本地推理的逻辑
- MAHPPO代码存在但没有集成到实验框架

#### 1.2.5 压缩率实验不完整

**缺失内容：**
- ❌ **不同分割点下的压缩率对比**：Point 1-4的压缩率对比（Proposed vs JALAD）
- ❌ **压缩率对累积奖励的影响**：不同压缩率下的训练曲线

**原因分析：**
- 当前实验主要关注推理性能，没有系统性地分析压缩率在不同分割点下的表现
- 没有实现JALAD方法进行对比

---

## 二、补充实验设计方案

### 2.1 训练过程数据记录增强

#### 2.1.1 累积奖励记录

**实现方案：**
```python
# 在train.py中添加
cumulative_rewards = []
current_cumulative = 0

for step in range(max_steps):
    # ... 训练代码 ...
    current_cumulative += reward
    if step % log_freq == 0:
        cumulative_rewards.append({
            'step': step,
            'cumulative_reward': current_cumulative,
            'avg_reward': np.mean(episode_rewards[-100:])
        })
```

**记录内容：**
- 每个时间步的累积奖励
- 滑动平均奖励（100步窗口）
- 保存为JSON格式，便于后续绘图

#### 2.1.2 Value Loss记录

**实现方案：**
```python
# 在PPO.update()方法中返回loss
def update(self, batch_size=64):
    # ... 更新代码 ...
    value_loss = F.mse_loss(new_values, returns)
    # 返回loss用于记录
    return value_loss.item()
```

**记录内容：**
- 每次更新时的Value Loss
- 记录到训练历史中

### 2.2 超参数敏感性实验

#### 2.2.1 学习率敏感性实验

**实验设计：**
- 学习率范围：`[0.00001, 0.0001, 0.001, 0.01]`
- 固定其他超参数
- 每个学习率训练500K步
- 记录Cumulative Reward和Value Loss

**实现脚本：** `experiments/hyperparameter_sensitivity.py`

#### 2.2.2 重用时间敏感性实验

**实验设计：**
- 重用时间（repeat_time/k_epochs）：`[5, 10, 20, 40, 80]`
- 固定其他超参数
- 记录Cumulative Reward

**实现脚本：** `experiments/reuse_time_sensitivity.py`

#### 2.2.3 内存大小敏感性实验

**实验设计：**
- Buffer大小（memory_size）：`[256, 512, 1024, 2048, 4096]`
- 固定其他超参数
- 记录Cumulative Reward和Value Loss

**实现脚本：** `experiments/memory_size_sensitivity.py`

### 2.3 多用户/多智能体实验

#### 2.3.1 多用户环境实现

**实现方案：**
- 扩展`CollaborativeInferenceEnv`支持多用户
- 实现多智能体PPO（参考MAHPPO-main代码）
- 支持3-10个用户的并发推理

**实现脚本：** `src/multi_agent_env.py`, `src/multi_agent_ppo.py`

#### 2.3.2 多用户性能评估

**实验设计：**
- 用户数量：`[3, 4, 5, 6, 7, 8, 9, 10]`
- 每个配置训练500K步
- 记录Cumulative Reward vs Time Frame

**实现脚本：** `experiments/multi_user_experiment.py`

### 2.4 对比基线实现

#### 2.4.1 Local基线

**实现方案：**
```python
class LocalBaseline:
    """完全本地推理基线"""
    def inference(self, model, data):
        # 在边缘设备上完成所有推理
        return model(data)
```

#### 2.4.2 JALAD基线

**实现方案：**
- 基于自编码器的特征压缩
- 固定压缩策略（不使用RL）
- 参考论文中的JALAD方法描述

**实现脚本：** `baselines/jalad.py`

#### 2.4.3 MAHPPO完整集成

**实现方案：**
- 集成MAHPPO-main代码到主框架
- 实现完整的训练和评估流程
- 与当前PPO方法对比

**实现脚本：** `baselines/mahppo_integrated.py`

### 2.5 压缩率对比实验

#### 2.5.1 不同分割点下的压缩率

**实验设计：**
- 分割点：Point 1, Point 2, Point 3, Point 4
- 方法：Proposed (RL), JALAD
- 记录压缩率（Compression Rate）

**实现脚本：** `experiments/compression_rate_comparison.py`

---

## 三、实验脚本设计

### 3.1 训练过程记录增强脚本

**文件：** `rl_collaborative_inference/train_with_tracking.py`

**功能：**
- 记录累积奖励
- 记录Value Loss
- 保存训练曲线数据
- 支持长时间训练（500K步）

### 3.2 超参数敏感性实验脚本

**文件：** `rl_collaborative_inference/experiments/hyperparameter_sensitivity.py`

**功能：**
- 自动运行不同超参数配置
- 收集所有实验结果
- 生成对比图表

### 3.3 多用户实验脚本

**文件：** `rl_collaborative_inference/experiments/multi_user_experiment.py`

**功能：**
- 支持不同用户数量
- 多智能体训练
- 性能对比分析

### 3.4 对比实验脚本

**文件：** `rl_collaborative_inference/experiments/comparison_experiment.py`

**功能：**
- Local基线
- JALAD基线
- MAHPPO方法
- Proposed方法（当前RL方法）
- 生成对比图表

### 3.5 论文图表生成脚本

**文件：** `rl_collaborative_inference/generate_paper_figures.py`

**功能：**
- 生成Cumulative Reward vs Time Frame图
- 生成超参数敏感性图
- 生成多用户性能图
- 生成压缩率对比图
- 生成Value Loss图

---

## 四、实施计划

### 阶段1：训练过程数据记录增强（优先级：高）
1. 修改`train.py`添加累积奖励记录
2. 修改`ppo.py`返回Value Loss
3. 测试数据记录功能

### 阶段2：超参数敏感性实验（优先级：高）
1. 实现超参数实验脚本
2. 运行学习率敏感性实验
3. 运行重用时间敏感性实验
4. 运行内存大小敏感性实验
5. 生成对比图表

### 阶段3：对比基线实现（优先级：中）
1. 实现Local基线
2. 实现JALAD基线
3. 集成MAHPPO
4. 运行对比实验

### 阶段4：多用户实验（优先级：中）
1. 扩展环境支持多用户
2. 实现多智能体PPO
3. 运行多用户实验
4. 生成性能对比图

### 阶段5：论文图表生成（优先级：高）
1. 整合所有实验数据
2. 生成符合论文风格的图表
3. 生成实验报告

---

## 五、预期实验结果

### 5.1 训练曲线图
- **Cumulative Reward vs Time Frame**：展示Local, JALAD, MAHPPO, Proposed的训练曲线
- **Value Loss vs Time Frame**：展示不同内存大小下的Value Loss变化

### 5.2 超参数敏感性图
- **学习率敏感性**：4个子图展示不同学习率的影响
- **重用时间敏感性**：5条曲线对比
- **内存大小敏感性**：5条曲线对比（Cumulative Reward和Value Loss）

### 5.3 多用户性能图
- **不同用户数量下的Cumulative Reward**：3-10用户的性能曲线

### 5.4 压缩率对比图
- **不同分割点下的压缩率**：Proposed vs JALAD的柱状图对比

---

## 六、注意事项

1. **训练时间**：500K步的训练需要较长时间，建议使用GPU加速
2. **数据存储**：所有实验数据需要保存，便于后续分析和图表生成
3. **可复现性**：设置随机种子，确保实验结果可复现
4. **资源管理**：多用户实验需要更多计算资源，注意资源分配
5. **基线实现**：JALAD和MAHPPO的实现需要参考原论文，确保正确性

