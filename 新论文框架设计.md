# 基于强化学习的协同推理框架设计
## 结合剪枝压缩与模型分割的优化方案

---

## 一、框架概述

### 1.1 研究目标

在Neurosurgeon和Multi-Agent论文的基础上，设计一个新的协同推理框架，主要特点：

1. **动作空间**：`(中间特征压缩率, 模型分割点)`
2. **奖励函数**：精度与时延的混合优化
3. **压缩机制**：使用真正的剪枝技术，云端可恢复
4. **分割算法**：基于强化学习的自适应分割点寻找

### 1.2 核心创新点

- ✅ **剪枝压缩**：使用结构化/非结构化剪枝压缩中间特征，而非自编码器
- ✅ **可恢复机制**：云端能够恢复被剪枝的特征，保证推理精度
- ✅ **混合优化**：同时优化推理精度和时延，而非仅优化时延
- ✅ **强化学习分割**：使用强化学习动态寻找最优分割点

---

## 二、状态空间设计（State Space）

### 2.1 状态空间组成

状态空间应包含能够全面反映系统当前状态的信息，设计如下：

#### **2.1.1 设备状态（Device State）**

```python
device_state = {
    'edge_cpu_util': float,      # 边缘设备CPU利用率 [0, 1]
    'edge_memory_util': float,    # 边缘设备内存利用率 [0, 1]
    'edge_battery': float,        # 边缘设备电池电量 [0, 1]
    'edge_compute_cap': float,    # 边缘设备计算能力（归一化）
    'cloud_cpu_util': float,       # 云端CPU利用率 [0, 1]
    'cloud_gpu_util': float,       # 云端GPU利用率 [0, 1]
    'cloud_memory_util': float,   # 云端内存利用率 [0, 1]
}
```

**维度**：7维

#### **2.1.2 网络状态（Network State）**

```python
network_state = {
    'bandwidth': float,          # 当前带宽 (MB/s) [归一化到0-1]
    'latency': float,             # 网络延迟 (ms) [归一化到0-1]
    'packet_loss': float,         # 丢包率 [0, 1]
    'signal_strength': float,     # 信号强度 [0, 1]
    'network_type': int,          # 网络类型 (0:WiFi, 1:LTE, 2:5G) [one-hot编码]
}
```

**维度**：5维（网络类型用3维one-hot编码，实际8维）

#### **2.1.3 任务特征（Task Features）**

```python
task_features = {
    'input_size': float,          # 输入数据大小 (MB) [归一化]
    'model_complexity': float,    # 模型复杂度（参数量/FLOPs） [归一化]
    'task_queue_length': int,     # 任务队列长度 [归一化]
    'expected_accuracy': float,   # 期望精度（基于历史数据） [0, 1]
}
```

**维度**：4维

#### **2.1.4 历史决策与性能（Historical Decisions & Performance）**

```python
history_state = {
    'last_partition_point': int,      # 上次分割点 [归一化到0-1]
    'last_compression_rate': float,   # 上次压缩率 [0, 1]
    'last_latency': float,            # 上次推理时延 [归一化到0-1]
    'last_accuracy': float,           # 上次推理精度 [0, 1]
    'avg_latency_window': float,     # 最近N次平均时延 [归一化]
    'avg_accuracy_window': float,    # 最近N次平均精度 [0, 1]
}
```

**维度**：6维

#### **2.1.5 中间特征信息（Intermediate Feature Info）**

```python
feature_state = {
    'feature_size': float,        # 中间特征大小 (MB) [归一化]
    'feature_channels': int,      # 中间特征通道数 [归一化]
    'feature_sparsity': float,    # 特征稀疏度（可用于剪枝） [0, 1]
    'compressibility': float,     # 可压缩性（基于特征统计） [0, 1]
}
```

**维度**：4维

### 2.2 状态空间总维度

**总维度**：7 + 8 + 4 + 6 + 4 = **29维**

### 2.3 状态归一化

所有状态值都应归一化到[0, 1]区间，便于神经网络处理：

```python
def normalize_state(state_dict):
    """
    归一化状态空间
    """
    normalized = {}
    
    # 设备状态归一化
    normalized['edge_cpu_util'] = state_dict['edge_cpu_util'] / 100.0
    normalized['edge_memory_util'] = state_dict['edge_memory_util'] / 100.0
    # ... 其他状态归一化
    
    return normalized
```

---

## 三、动作空间设计（Action Space）

### 3.1 动作空间组成

动作空间为**混合动作空间**，包含两个部分：

#### **3.1.1 模型分割点（Partition Point）**

- **类型**：离散动作（Discrete Action）
- **取值范围**：`[0, 1, 2, ..., N-1]`，其中N为模型可分割层数
- **含义**：
  - `0`：完全云端推理（边缘端不执行）
  - `1 to N-2`：在第i层后分割
  - `N-1`：完全边缘推理（云端不执行）

**示例**：对于ResNet18，可能有6个候选分割点（0-5）

#### **3.1.2 中间特征压缩率（Compression Rate）**

- **类型**：连续动作（Continuous Action）
- **取值范围**：`[0.1, 1.0]`（10%到100%，即压缩10倍到不压缩）
- **含义**：压缩率越小，压缩程度越大，传输数据越少，但可能影响精度

**注意**：压缩率通过剪枝实现，具体见第四部分

### 3.2 动作空间表示

```python
action = {
    'partition_point': int,        # 离散：分割点索引
    'compression_rate': float,    # 连续：压缩率 [0.1, 1.0]
}
```

### 3.3 Actor网络设计

参考MAHPPO的混合动作空间处理：

```python
class Actor(nn.Module):
    def __init__(self, num_states, num_partition_points, compression_min=0.1, compression_max=1.0):
        super(Actor, self).__init__()
        self.base = nn.Sequential(
            nn.Linear(num_states, 256),
            nn.ReLU(),
            nn.Linear(256, 128),
            nn.ReLU()
        )
        
        # 分割点输出（离散）
        self.partition_header = nn.Sequential(
            nn.Linear(128, 64),
            nn.ReLU(),
            nn.Linear(64, num_partition_points),
            nn.Softmax(dim=-1)
        )
        
        # 压缩率输出（连续）
        self.compression_mu_header = nn.Sequential(
            nn.Linear(128, 64),
            nn.ReLU(),
            nn.Linear(64, 1),
            nn.Sigmoid()
        )
        self.compression_sigma_header = nn.Sequential(
            nn.Linear(128, 64),
            nn.ReLU(),
            nn.Linear(64, 1),
            nn.Softplus()
        )
        
        self.compression_min = compression_min
        self.compression_max = compression_max
    
    def forward(self, state):
        code = self.base(state)
        
        # 分割点概率分布
        prob_partition = self.partition_header(code)
        
        # 压缩率（高斯分布参数）
        compression_mu = self.compression_mu_header(code) * (self.compression_max - self.compression_min) + self.compression_min
        compression_sigma = self.compression_sigma_header(code)
        
        return prob_partition, (compression_mu, compression_sigma)
```

---

## 四、剪枝压缩机制设计

### 4.1 剪枝策略

#### **4.1.1 结构化剪枝（Structured Pruning）**

对中间特征张量进行通道级剪枝：

```python
def structured_prune(feature_tensor, compression_rate):
    """
    结构化剪枝：按通道剪枝
    :param feature_tensor: 中间特征 [B, C, H, W]
    :param compression_rate: 压缩率 [0.1, 1.0]
    :return: 剪枝后的特征, 剪枝掩码
    """
    B, C, H, W = feature_tensor.shape
    
    # 计算保留的通道数
    keep_channels = int(C * compression_rate)
    keep_channels = max(1, keep_channels)  # 至少保留1个通道
    
    # 计算每个通道的重要性（使用L2范数）
    channel_importance = torch.norm(feature_tensor, dim=(0, 2, 3))  # [C]
    
    # 选择最重要的通道
    _, top_indices = torch.topk(channel_importance, keep_channels)
    top_indices = torch.sort(top_indices)[0]  # 排序以保持顺序
    
    # 创建掩码
    mask = torch.zeros(C, dtype=torch.bool, device=feature_tensor.device)
    mask[top_indices] = True
    
    # 剪枝
    pruned_feature = feature_tensor[:, mask, :, :]
    
    return pruned_feature, mask, top_indices
```

#### **4.1.2 非结构化剪枝（Unstructured Pruning）**

对中间特征张量进行元素级剪枝：

```python
def unstructured_prune(feature_tensor, compression_rate):
    """
    非结构化剪枝：按元素剪枝
    :param feature_tensor: 中间特征 [B, C, H, W]
    :param compression_rate: 压缩率 [0.1, 1.0]
    :return: 剪枝后的特征（稀疏张量）, 剪枝掩码
    """
    # 计算保留的元素数
    total_elements = feature_tensor.numel()
    keep_elements = int(total_elements * compression_rate)
    keep_elements = max(1, keep_elements)
    
    # 计算每个元素的重要性（使用绝对值）
    importance = torch.abs(feature_tensor.flatten())
    
    # 选择最重要的元素
    _, top_indices = torch.topk(importance, keep_elements)
    
    # 创建掩码
    mask = torch.zeros_like(feature_tensor, dtype=torch.bool)
    mask_flatten = mask.flatten()
    mask_flatten[top_indices] = True
    mask = mask_flatten.reshape(feature_tensor.shape)
    
    # 剪枝（使用稀疏张量）
    pruned_feature = feature_tensor * mask
    
    return pruned_feature, mask, top_indices
```

### 4.2 云端恢复机制

#### **4.2.1 结构化剪枝恢复**

```python
def structured_recover(pruned_feature, mask, original_channels, device):
    """
    恢复结构化剪枝的特征
    :param pruned_feature: 剪枝后的特征 [B, C', H, W]
    :param mask: 剪枝掩码 [C]
    :param original_channels: 原始通道数
    :param device: 设备
    :return: 恢复后的特征 [B, C, H, W]
    """
    B, C_pruned, H, W = pruned_feature.shape
    recovered = torch.zeros(B, original_channels, H, W, device=device)
    
    # 将剪枝后的特征放回原位置
    recovered[:, mask, :, :] = pruned_feature
    
    return recovered
```

#### **4.2.2 非结构化剪枝恢复**

```python
def unstructured_recover(pruned_feature, mask, device):
    """
    恢复非结构化剪枝的特征
    :param pruned_feature: 剪枝后的稀疏特征 [B, C, H, W]
    :param mask: 剪枝掩码 [B, C, H, W]
    :param device: 设备
    :return: 恢复后的特征 [B, C, H, W]
    """
    # 非结构化剪枝的恢复就是直接使用掩码
    # 因为稀疏张量已经包含了位置信息
    recovered = pruned_feature.to_dense() if pruned_feature.is_sparse else pruned_feature
    return recovered
```

### 4.3 剪枝信息传输

需要传输到云端的信息：

```python
pruning_info = {
    'pruned_feature': torch.Tensor,    # 剪枝后的特征
    'mask': torch.Tensor,               # 剪枝掩码
    'pruning_type': str,                # 'structured' or 'unstructured'
    'original_shape': tuple,            # 原始特征形状
    'compression_rate': float,          # 压缩率
}
```

**传输优化**：只传输剪枝后的特征和掩码，掩码可以用稀疏格式压缩

---

## 五、奖励函数设计（Reward Function）

### 5.1 奖励函数组成

奖励函数综合考虑**精度**和**时延**：

```python
def compute_reward(accuracy, latency, target_accuracy=0.95, max_latency=1.0, 
                   alpha=0.6, beta=0.4, accuracy_weight=1.0, latency_weight=1.0):
    """
    计算奖励函数
    :param accuracy: 推理精度 [0, 1]
    :param latency: 推理时延 (秒) [归一化到0-1]
    :param target_accuracy: 目标精度阈值
    :param max_latency: 最大可接受时延
    :param alpha: 精度权重
    :param beta: 时延权重（alpha + beta = 1）
    :param accuracy_weight: 精度缩放因子
    :param latency_weight: 时延缩放因子
    :return: 奖励值
    """
    # 精度奖励（越高越好）
    if accuracy >= target_accuracy:
        accuracy_reward = accuracy_weight * (accuracy - target_accuracy) / (1 - target_accuracy)
    else:
        # 精度低于阈值时，惩罚更大
        accuracy_reward = -accuracy_weight * (target_accuracy - accuracy) / target_accuracy * 2
    
    # 时延奖励（越低越好，转换为负值）
    latency_reward = -latency_weight * (latency / max_latency)
    
    # 综合奖励
    reward = alpha * accuracy_reward + beta * latency_reward
    
    return reward
```

### 5.2 奖励函数变体

#### **5.2.1 基于阈值的奖励**

```python
def threshold_based_reward(accuracy, latency, target_accuracy=0.95, max_latency=1.0):
    """
    基于阈值的奖励函数
    """
    # 精度必须达到阈值，否则大幅惩罚
    if accuracy < target_accuracy:
        return -10.0  # 大幅惩罚
    
    # 时延必须在可接受范围内
    if latency > max_latency:
        return -5.0  # 中等惩罚
    
    # 在阈值内，奖励与精度正相关，与时延负相关
    reward = accuracy - (latency / max_latency)
    return reward
```

#### **5.2.2 多目标优化奖励**

```python
def multi_objective_reward(accuracy, latency, energy=None, 
                          w_accuracy=0.5, w_latency=0.3, w_energy=0.2):
    """
    多目标优化奖励函数
    """
    accuracy_reward = w_accuracy * accuracy
    latency_reward = -w_latency * latency  # 时延越小越好
    energy_reward = -w_energy * energy if energy is not None else 0
    
    reward = accuracy_reward + latency_reward + energy_reward
    return reward
```

### 5.3 奖励归一化

为了稳定训练，可以对奖励进行归一化：

```python
def normalize_reward(reward, reward_history, window_size=100):
    """
    使用滑动窗口归一化奖励
    """
    reward_history.append(reward)
    if len(reward_history) > window_size:
        reward_history.pop(0)
    
    mean_reward = np.mean(reward_history)
    std_reward = np.std(reward_history)
    
    if std_reward > 1e-6:
        normalized_reward = (reward - mean_reward) / std_reward
    else:
        normalized_reward = reward - mean_reward
    
    return normalized_reward
```

---

## 六、新的分割点寻找算法

### 6.1 基于强化学习的自适应分割算法

#### **6.1.1 算法思想**

不同于Neurosurgeon的贪心搜索，新算法使用强化学习动态学习最优分割点：

1. **探索阶段**：智能体探索不同的分割点和压缩率组合
2. **学习阶段**：根据奖励信号学习最优策略
3. **适应阶段**：根据环境变化（网络、设备状态）自适应调整

#### **6.1.2 算法流程**

```python
class AdaptivePartitionAgent:
    def __init__(self, model, state_dim, action_dim):
        self.model = model
        self.actor = Actor(state_dim, num_partition_points, ...)
        self.critic = Critic(state_dim)
        self.optimizer = ...
        
    def select_partition_point(self, state):
        """
        基于当前状态选择分割点
        """
        # 获取动作概率分布
        prob_partition, (compression_mu, compression_sigma) = self.actor(state)
        
        # 采样分割点和压缩率
        partition_point = Categorical(prob_partition).sample()
        compression_rate = Normal(compression_mu, compression_sigma).sample()
        compression_rate = torch.clamp(compression_rate, 0.1, 1.0)
        
        return partition_point.item(), compression_rate.item()
    
    def update_policy(self, states, actions, rewards):
        """
        更新策略网络
        """
        # 计算优势函数
        values = self.critic(states)
        advantages = rewards - values
        
        # PPO更新
        # ... (参考MAHPPO的实现)
```

### 6.2 参考CoEdge的自适应工作负载分区

#### **6.2.1 CoEdge的核心思想**

- **异构设备协作**：考虑不同设备的计算能力
- **动态负载均衡**：根据设备状态动态分配工作负载
- **自适应分区**：根据网络条件和设备能力调整分区策略

#### **6.2.2 结合CoEdge的改进**

```python
class CoEdgeInspiredPartition:
    def __init__(self):
        self.device_capabilities = {}  # 设备能力字典
        self.network_conditions = {}   # 网络条件字典
        
    def adaptive_partition(self, model, edge_capability, cloud_capability, 
                          network_bandwidth, compression_rate):
        """
        自适应分区算法
        :param edge_capability: 边缘设备计算能力
        :param cloud_capability: 云端计算能力
        :param network_bandwidth: 网络带宽
        :param compression_rate: 压缩率
        """
        # 计算各层的计算开销
        layer_costs = self.compute_layer_costs(model)
        
        # 考虑设备能力和网络条件
        edge_speed = edge_capability['compute_speed']
        cloud_speed = cloud_capability['compute_speed']
        transmission_cost = self.compute_transmission_cost(
            compression_rate, network_bandwidth
        )
        
        # 动态寻找最优分割点
        best_partition = self.find_optimal_partition(
            layer_costs, edge_speed, cloud_speed, transmission_cost
        )
        
        return best_partition
    
    def find_optimal_partition(self, layer_costs, edge_speed, cloud_speed, transmission_cost):
        """
        寻找最优分割点（结合强化学习）
        """
        # 使用强化学习智能体选择分割点
        state = self.build_state(layer_costs, edge_speed, cloud_speed, transmission_cost)
        partition_point, compression_rate = self.agent.select_action(state)
        
        return partition_point, compression_rate
```

### 6.3 分层搜索策略

结合贪心搜索和强化学习：

```python
class HybridPartitionSearch:
    def __init__(self):
        self.rl_agent = AdaptivePartitionAgent(...)
        self.greedy_search = GreedyPartitionSearch(...)
        
    def search_partition_point(self, state, use_rl=True):
        """
        混合搜索策略
        """
        if use_rl:
            # 使用强化学习（在线学习）
            partition_point, compression_rate = self.rl_agent.select_partition_point(state)
        else:
            # 使用贪心搜索（离线预计算）
            partition_point, compression_rate = self.greedy_search.search(state)
        
        return partition_point, compression_rate
```

---

## 七、完整系统架构

### 7.1 系统流程图

```
输入图像
    ↓
边缘设备
    ↓
[模型分割点选择] ← 强化学习智能体
    ↓
边缘端推理（前N层）
    ↓
[中间特征生成]
    ↓
[剪枝压缩] ← 压缩率动作
    ↓
[传输到云端]（传输剪枝后的特征+掩码）
    ↓
云端
    ↓
[恢复中间特征]（使用掩码恢复）
    ↓
云端推理（后M层）
    ↓
[输出结果]
    ↓
[计算奖励]（精度 + 时延）
    ↓
[更新策略]（PPO算法）
```

### 7.2 关键模块

1. **状态观测模块**：收集设备、网络、任务状态
2. **动作选择模块**：Actor网络选择分割点和压缩率
3. **模型分割模块**：根据分割点划分模型
4. **剪枝压缩模块**：对中间特征进行剪枝
5. **传输模块**：传输剪枝后的特征和掩码
6. **恢复模块**：云端恢复中间特征
7. **推理模块**：边缘端和云端协同推理
8. **奖励计算模块**：计算精度和时延奖励
9. **策略更新模块**：使用PPO更新Actor和Critic

---

## 八、实验设计

### 8.1 数据集

- **Caltech-101**：101个类别的图像分类数据集
- **数据预处理**：224x224图像，数据增强

### 8.2 模型选择

- ResNet18
- VGG11
- MobileNetV2
- AlexNet

### 8.3 评估指标

1. **推理精度**：Top-1准确率
2. **推理时延**：端到端延迟（边缘推理+传输+云端推理）
3. **传输开销**：剪枝后的特征大小
4. **能耗**：边缘设备和云端的能耗
5. **压缩比**：原始特征大小 / 剪枝后特征大小

### 8.4 对比基线

1. **Neurosurgeon**：静态分割，无压缩
2. **Multi-Agent**：自编码器压缩，多智能体
3. **CoEdge**：自适应工作负载分区
4. **全边缘推理**：所有计算在边缘
5. **全云端推理**：所有计算在云端

---

## 九、实现细节

### 9.1 环境类设计

```python
class CollaborativeInferenceEnv:
    def __init__(self, model, dataset, edge_device, cloud_device):
        self.model = model
        self.dataset = dataset
        self.edge_device = edge_device
        self.cloud_device = cloud_device
        
    def reset(self):
        """重置环境"""
        # 加载新样本
        # 初始化状态
        return state
    
    def step(self, action):
        """
        执行一步推理
        :param action: (partition_point, compression_rate)
        :return: next_state, reward, done, info
        """
        # 1. 分割模型
        edge_model, cloud_model = self.partition_model(action['partition_point'])
        
        # 2. 边缘端推理
        edge_output = self.edge_inference(edge_model, input_data)
        
        # 3. 剪枝压缩
        pruned_feature, mask = self.prune_feature(edge_output, action['compression_rate'])
        
        # 4. 传输
        transmission_time = self.transmit(pruned_feature, mask)
        
        # 5. 云端恢复
        recovered_feature = self.recover_feature(pruned_feature, mask)
        
        # 6. 云端推理
        cloud_output = self.cloud_inference(cloud_model, recovered_feature)
        
        # 7. 计算奖励
        accuracy = self.compute_accuracy(cloud_output, ground_truth)
        latency = self.compute_latency(edge_time, transmission_time, cloud_time)
        reward = self.compute_reward(accuracy, latency)
        
        # 8. 更新状态
        next_state = self.update_state()
        
        return next_state, reward, done, info
```

### 9.2 训练流程

```python
def train_rl_agent():
    env = CollaborativeInferenceEnv(...)
    agent = PPOAgent(...)
    
    for episode in range(num_episodes):
        state = env.reset()
        episode_reward = 0
        
        for step in range(max_steps):
            # 选择动作
            action = agent.select_action(state)
            
            # 执行动作
            next_state, reward, done, info = env.step(action)
            
            # 存储经验
            agent.store_transition(state, action, reward, next_state, done)
            
            state = next_state
            episode_reward += reward
            
            if done:
                break
        
        # 更新策略
        if len(agent.buffer) >= batch_size:
            agent.update()
        
        # 记录日志
        logger.info(f"Episode {episode}, Reward: {episode_reward}")
```

---

## 十、预期贡献

1. **剪枝压缩机制**：首次在协同推理中使用真正的剪枝技术
2. **可恢复剪枝**：保证剪枝后仍能恢复特征，维持精度
3. **混合优化**：同时优化精度和时延，而非仅优化时延
4. **强化学习分割**：使用RL动态寻找最优分割点
5. **实验验证**：在Caltech-101上验证方法的有效性

---

## 十一、总结

本框架结合了Neurosurgeon的模型分割思想、Multi-Agent的强化学习方法和CoEdge的自适应分区策略，并引入了剪枝压缩机制，实现了精度与时延的混合优化。通过强化学习动态选择分割点和压缩率，能够适应动态的网络和设备环境，实现高效的协同推理。

